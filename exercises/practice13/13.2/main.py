# Définissez une fonction `process_string` qui prend une chaîne de caractères et renvoie une nouvelle chaîne de caractères qui a été convertie en minuscules et dont les espaces blancs excédentaires ont été supprimés.

def process_string(raw_string):
    return raw_string.strip().lower()